# Local testing only — copy this file to .env and fill in values
# Never commit .env to git

GMAIL_USER=cunningham.matthew91@gmail.com
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
RECIPIENT_EMAIL=cunningham.matthew91@gmail.com

# Claude enrichment (clustering, ranking, summaries, sectors).
# If unset, the digest still sends using the deterministic fallback ranker.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
# Optional overrides:
# DIGEST_MODEL=claude-opus-4-8
# DIGEST_EFFORT=high
# DIGEST_DRY_RUN=1   # render data/preview.html and skip sending
